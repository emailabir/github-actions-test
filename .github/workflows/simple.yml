name: shell commands

on: [push]

jobs:
  run-shell-command:
    runs-on:  ubantu-latest
    steps:
      - name: echo a string
        run: echo "Hello World"
      - name: multiline scripts
        run: |
          node -v
          npm -v